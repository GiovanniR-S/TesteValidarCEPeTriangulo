@{
    ViewData["Title"] = "Lista de CEP";
}

<div class="align-content-center mb-3">
    <a href="@Url.Action("ValidarCEP", "CEP")" class="btn btn-primary">Adicionar novo CEP</a>
</div>

@if(ViewBag.Cep != null) {
    <h2>Lista completa</h2>
    <table class="table">
        <tr>
            <th>CEP</th>
            <th>Logradouro</th>
            <th>Complemento</th>
            <th>Bairro</th>
            <th>Localidade</th>
            <th>UF</th>
            <th>Unidade</th>
            <th>IBGE</th>
            <th>GIA</th>
        </tr>
        @foreach(var item in ViewBag.Cep) {
            <tr>
                <td><a href="@Url.Action("ExibirEndereco", "CEP")/@item.Cep">@item.Cep</a></td>
                <td>@item.Logradouro</td>
                <td>@item.Complemento</td>
                <td>@item.Bairro</td>
                <td>@item.Localidade</td>
                <td>@item.UF</td>
                <td>@item.Unidade</td>
                <td>@item.IBGE</td>
                <td>@item.GIA</td>
            </tr>
        }
    </table>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
